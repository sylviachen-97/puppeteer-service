{
  "name": "my-puppeteer-service",
  "version": "1.0.0",
  "description": "Puppeteer 18 on Render",
  "scripts": {
    // No "build" script is strictly needed.
    // Render by default runs `npm install`, then `npm start`.
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    // Pin Puppeteer to 18.2.1
    "puppeteer": "18.2.1"
  }
}